# Railway.app Configuration for CountryFarm Backend
# This file helps Railway auto-detect and deploy the application

[build]
builder = "nixpacks"

[deploy]
startCommand = "npm start"
healthcheckPath = "/api/health"
healthcheckTimeout = 10
restartPolicyMaxRetries = 5
